import { Text, View } from "react-native";
import tw from "@lm/configs/tailwindcss";

const NavBar = () => {
  return (
    <View>
      <Text style={tw`text-white text-sm`}>Navbar</Text>
    </View>
  );
};

export default NavBar;
